name: Main-IO CI

on: push

jobs:
  WubbaLubbaDubDub:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Setup
      run: sudo apt install cmake -y && ./dev.sh setup
    - name: Build
      run: ./dev.sh build
